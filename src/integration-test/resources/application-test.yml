spring:
  config:
    activate:
      on-profile: test

keycloak:
  server-url: http://localhost:8080
  realm: master
  user-realm: local-realm
  grant-type: password
  client-id: admin-cli
  user-client-id: local-client
  username: admin
  password: admin